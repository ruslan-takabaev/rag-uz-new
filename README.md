[TO BE UPDATED]

0) Extract laws from zip archive into your working directory
  
1) Install dependencies from requirements.txt
   
2) Run the build_index.py app (UPDATE LLM ENDPOINTS AND API KEYS)

3) After faiss index is built, run the main app (will not work without .index files generated in previous step)
